{
  "name": "dieta-substituicao-web",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.7.0",
    "surge": "^0.18.0"
  },
  "dependencies": {
    "mathjs": "^3.7.0",
    "numeral": "^1.5.3",
    "react": "^15.3.2",
    "react-dom": "^15.3.2",
    "semantic-ui-css": "^2.2.4",
    "semantic-ui-react": "^0.60.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && mv build/index.html build/200.html && surge --domain superb-feather.surge.sh --project ./build"
  }
}
